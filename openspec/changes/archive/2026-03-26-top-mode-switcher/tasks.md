## 1. 顶部 Tab 栏

- [x] 1.1 新增顶部 Tab 栏组件：左侧为项目信息（返回按钮、头像、名称、状态标签），右侧为「控制台」「代码模式」Tab 和「去对话」按钮
- [x] 1.2 实现 Tab 切换逻辑，默认选中「控制台」

## 2. 移除侧边导航中的模式切换

- [x] 2.1 移除侧边导航中的「代码模式」和「控制台」切换按钮
- [x] 2.2 移除侧边导航中的「去对话」按钮（已移至顶部 Tab 栏）
- [x] 2.3 移除侧边导航折叠逻辑（`.collapsed` class、`v-if="!isCodeMode"` 条件、transition 动画）

## 3. 代码模式布局调整

- [x] 3.1 代码模式下通过 `v-if` 完全隐藏侧边导航，内容区通过 flex 自动占满全宽
- [x] 3.2 清理不再使用的折叠相关样式（`.collapsed`、侧边导航 transition、折叠导航样式）
