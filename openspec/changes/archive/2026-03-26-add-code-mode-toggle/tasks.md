## 1. 模式状态管理

- [x] 1.1 添加 `isCodeMode` ref 状态，默认为 `false`（控制台模式）
- [x] 1.2 添加 `toggleCodeMode()` 和 `setConsoleMode()` 方法

## 2. 侧边导航模式切换控件

- [x] 2.1 在侧边导航顶部项目信息区添加「代码模式」切换按钮（控制台模式下显示）
- [x] 2.2 代码模式下侧边导航折叠为 60px 窄条，仅显示返回按钮、「控制台」切换按钮和纵向菜单图标

## 3. 代码模式布局

- [x] 3.1 侧边导航宽度变化添加 CSS transition 动画（0.3s）
- [x] 3.2 代码模式下折叠导航菜单图标仅做展示，不响应点击操作
- [x] 3.3 代码模式下 iframe src 固定指向 `/code` 路径，切回控制台时恢复原菜单项对应的路径

## 4. 内容区适配

- [x] 4.1 内容区通过 flex 布局自动跟随侧边导航宽度变化
- [x] 4.2 模式切换时触发 iframe 重新加载（骨架屏动画）
